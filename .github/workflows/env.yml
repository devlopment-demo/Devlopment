
name: Create Dev Environment
on:
  push:
    branches:
      - main
jobs:
  create-dev-environment:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v2

      - name: Create Dev Environment
        run: |
          # Add commands to create the "dev" environment here
          echo "Creating Dev Environment"
